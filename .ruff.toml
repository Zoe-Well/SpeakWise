# Ruff linting configuration for SpeakWise backend
target-version = "py312"

[lint]
select = ["E", "F", "I", "N", "W", "UP", "B"]
ignore = ["E501"]  # line length handled by formatter

[format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"
